version: 2.1
executors:
  my-custom-executor:
    docker:
      - image: cimg/openjdk:18.0.2
        auth:
          # ensure you have first added these secrets
          # visit app.circleci.com/settings/project/github/VelocityPackage/webJ/environment-variables
          username: $DOCKER_HUB_USER
          password: $DOCKER_HUB_PASSWORD
jobs:
  build:

    executor: my-custom-executor
    steps:
      - checkout
      - run: |
          # mvn build

workflows:
  my-custom-workflow:
    jobs:
      - build